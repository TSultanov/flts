[workspace]
resolver = "3"
members = ["cli", "library", "site/src-tauri"]

[profile.release]
opt-level = 3
lto = "thin"
panic = "abort"
strip = "symbols"

[profile.release-ship]
inherits = "release"
lto = "fat"
codegen-units = 1

