\* MC_hunt_f1.cfg — Hunt for F1: Stale Library Reference
\* FIXED: ConfigChange now aborts worker tasks (JoinHandle::abort on Drop).
\* This config should produce NO violations.

SPECIFICATION MCSpec

CONSTANTS
    Task = {t1, t2}
    Book = {b1}
    ConfigChangeLimit = 2
    AppCloseLimit = 0
    MaxTruthVersion = 8

CONSTRAINT StateConstraint

INVARIANTS
    MCPCConsistency
    MCTaskLibraryValidity
    MCStaleLibrarySafety
    MCNoDataLoss
