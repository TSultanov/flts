SPECIFICATION MCSpec

CONSTANTS
    Task = {t1, t2}
    Book = {b1}
    ConfigChangeLimit = 1
    AppCloseLimit = 0
    MaxTruthVersion = 6

CONSTRAINT StateConstraint

\* ========================================================================
\* Structural invariants (always checked)
\* ========================================================================
INVARIANTS
    MCPCConsistency
    MCTaskLibraryValidity

\* ========================================================================
\* Standard safety invariants
\* ========================================================================
INVARIANTS
    MCEventMonotonicity
    MCUIConsistency

\* ========================================================================
\* Extension invariants (bug-family specific — enable for hunting)
\* ========================================================================
\* INVARIANT MCStaleLibrarySafety
\* INVARIANT MCNoDataLoss
\* INVARIANT MCNoPersistenceLoss
\* INVARIANT MCNoStaleTranslation
