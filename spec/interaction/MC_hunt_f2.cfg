\* MC_hunt_f2.cfg — Hunt for F2: Event Ordering / Stale Snapshots
\* FIXED: library_updated and config_updated switched from eventToReadable
\* to getterToReadableWithEvents (signal-only → re-fetch via invoke).
\* EventMonotonicity and UIConsistency should now pass (no violations).

SPECIFICATION MCSpec

CONSTANTS
    Task = {t1, t2, t3}
    Book = {b1}
    ConfigChangeLimit = 0
    AppCloseLimit = 0
    MaxTruthVersion = 6

CONSTRAINT StateConstraint

INVARIANTS
    MCPCConsistency
    MCTaskLibraryValidity
    MCEventMonotonicity
    MCUIConsistency
