\* Family 2 hunt — reconcile must never tear down an active device.
\*
\* Open question (brief Family 2): the reconcile plan removes only engine devices
\* that the merged roster TOMBSTONES and does not re-list; a device active in the
\* roster must never appear in the remove set, in any interleaving of add/remove
\* and roster sync. ReconcileNeverDropsActive is a pure-function safety property
\* that must hold in every reachable state.
\*
\* Setup: concurrent add/remove of the same device on different nodes, plus syncs,
\* so the roster reaches mixed active/tombstone states before reconcile runs.

CONSTANTS
    Node = {n1, n2, n3}
    MaxClock = 2

    MaxPairLimit       = 2
    MaxUnpairLimit     = 2
    MaxApproveLimit    = 2
    MaxEnsureSelfLimit = 0

SPECIFICATION MCSpec
CHECK_DEADLOCK FALSE
SYMMETRY Symmetry
VIEW View

INVARIANTS
    TypeOK
    ReconcileNeverDropsActive
