\* Base spec config — for direct SPECIFICATION checking (not model checking)

SPECIFICATION Spec

CONSTANTS
    Task = {t1, t2, t3}
    Book = {b1}
    Translation = {tr1}
    Dictionary = {d1}
    Paragraph = {p1, p2}
    MaxBookContention = 3

INVARIANTS
    MutexExclusivity
    PCConsistency
    NoDeadlock
    LockOrderConsistency
    NoSelfDeadlock
